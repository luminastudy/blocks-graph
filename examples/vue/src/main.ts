import { createApp } from 'vue'
import './style.css'
import App from './App.vue'

/**
 * Vue 3 Entry Point:
 * Creates and mounts the root Vue application
 *
 * Uses the BlocksGraphVue wrapper component for clean, Vue-style integration
 */
createApp(App).mount('#app')
