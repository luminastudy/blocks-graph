import { describe, it, expect } from 'vitest'
import { addBlockWithSubBlocks } from './add-block-with-sub-blocks.js'

describe('addBlockWithSubBlocks', () => {
  it('should be defined', () => {
    expect(addBlockWithSubBlocks).toBeDefined()
  })
})
