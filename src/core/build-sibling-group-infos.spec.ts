import { describe, it, expect } from 'vitest'
import { buildSiblingGroupInfos } from './build-sibling-group-infos.js'

describe('buildSiblingGroupInfos', () => {
  it('should be defined', () => {
    expect(buildSiblingGroupInfos).toBeDefined()
  })
})
