import { describe, it, expect } from 'vitest'
import { calculateBlockPosition } from './calculate-block-position.js'

describe('calculateBlockPosition', () => {
  it('should be defined', () => {
    expect(calculateBlockPosition).toBeDefined()
  })
})
