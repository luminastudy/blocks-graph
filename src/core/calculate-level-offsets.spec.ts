import { describe, it, expect } from 'vitest'
import { calculateLevelOffsets } from './calculate-level-offsets.js'

describe('calculateLevelOffsets', () => {
  it('should be defined', () => {
    expect(calculateLevelOffsets).toBeDefined()
  })
})
