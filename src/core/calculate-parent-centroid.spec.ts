import { describe, it, expect } from 'vitest'
import { calculateParentCentroid } from './calculate-parent-centroid.js'

describe('calculateParentCentroid', () => {
  it('should be defined', () => {
    expect(calculateParentCentroid).toBeDefined()
  })
})
