import { describe, it, expect } from 'vitest'
import { resolveVerticalOverlaps } from './resolve-vertical-overlaps.js'

describe('resolveVerticalOverlaps', () => {
  it('should be defined', () => {
    expect(resolveVerticalOverlaps).toBeDefined()
  })
})
